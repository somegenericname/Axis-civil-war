#category for GER
GER_subjugate_pakt = {
	
	GER_sub_holland = {
		
		icon = generic_oppression

		allowed = {
			tag = GER
		}

		available = {
			HOL = {
				is_subject_of = GER
			}
			has_equipment = { infantry_equipment > 4999 }
		}

		cost = 25

		days_remove = 20
		days_re_enable = 120

		remove_effect = {
			HOL = {
				add_war_support = -0.1
			}
		}



	}
}
